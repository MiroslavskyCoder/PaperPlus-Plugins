# 📜 Скрипты LoaderScript

Эта папка содержит JavaScript скрипты для использования с плагином **LoaderScript** на Minecraft сервере Paper/Spigot с установленным модулем **LXXV Common**.

## 🚀 Установка

1. Убедитесь, что у вас установлен плагин **LoaderScript**
2. Убедитесь, что установлен модуль **LXXV Common** (содержит LXXVServer API)
3. Скопируйте скрипты в папку `plugins/LoaderScript/scripts/`
4. Перезагрузите сервер или выполните команду `/loaderscript reload`

## 📦 Доступные скрипты

### 1. `welcome.js` - Приветствие игроков
Отправляет приветственное сообщение новым игрокам с титрами, звуками и эффектами.

**Возможности:**
- Приветственное сообщение в чате
- Титр с именем игрока
- Звук приветствия
- Подсказка через 3 секунды
- Объявление о входе игрока

**API используемые методы:**
- `LXXVServer.on('playerJoin', callback)`
- `LXXVServer.sendMessage()`
- `LXXVServer.sendTitle()`
- `LXXVServer.playSound()`
- `LXXVServer.runTaskLater()`
- `LXXVServer.broadcast()`

---

### 2. `daily-rewards.js` - Ежедневные награды
Система ежедневных наград с бонусами за серии входов.

**Возможности:**
- Награда 1000$ за ежедневный вход
- Бонус +500$ за серию
- Проверка кулдауна (24 часа)
- Команда `/dailycheck` для проверки статуса
- Интеграция с Vault (экономика)

**API используемые методы:**
- `LXXVServer.getBalance()`
- `LXXVServer.deposit()`
- `LXXVServer.spawnParticle()`
- `LXXVServer.registerCommand()`

**Требования:**
- Плагин Vault
- Плагин экономики (например, EssentialsX Economy)

---

### 3. `custom-events.js` - Кастомные ивенты
Автоматические случайные ивенты на сервере с Boss Bar и эффектами.

**Ивенты:**
- **Метеоритный дождь** (5 мин, кулдаун 30 мин)
  - Падающие метеориты с ресурсами
  - Boss Bar с прогрессом
  - Звуки и частицы
  
- **Благословение** (10 мин, кулдаун 1 час)
  - Удвоенный опыт
  - Эффект удачи

**Команды:**
- `/startevent meteor` - Запустить метеоритный дождь
- `/startevent blessing` - Запустить благословение

**Права доступа:**
- `admin.events` - Для запуска ивентов

**API используемые методы:**
- `LXXVServer.createBossBar()`
- `LXXVServer.showBossBar()`
- `LXXVServer.removeBossBar()`
- `LXXVServer.runTaskTimer()`
- `LXXVServer.getOnlinePlayers()`

---

### 4. `auto-restart.js` - Автоматический перезапуск
Автоматический перезапуск сервера по расписанию с предупреждениями.

**Возможности:**
- Перезапуск в 04:00 и 16:00 (по умолчанию)
- Предупреждения: 10 мин, 5 мин, 3 мин, 1 мин, 30с, 10с, 5с
- Титры для последних 30 секунд
- Автоматическое сохранение миров
- Кик всех игроков с сообщением

**Команды:**
- `/restart` - Показать время до рестарта
- `/restart now` - Принудительный рестарт (админ)

**Права доступа:**
- `admin.restart` - Для принудительного рестарта

**API используемые методы:**
- `LXXVServer.saveAllWorlds()`
- `LXXVServer.kickPlayer()`
- `LXXVServer.executeCommand()`

**Настройка:**
Измените массив `RESTART_TIMES` в начале скрипта:
```javascript
const RESTART_TIMES = ['04:00', '16:00', '22:00']; // Добавить время
```

---

### 5. `scoreboard-manager.js` - Менеджер скорборда
Динамический скорборд для каждого игрока с информацией.

**Отображаемая информация:**
- Имя игрока
- Здоровье (с прогресс-баром)
- Уровень
- Баланс (если Vault доступен)
- Текущий мир
- Игроки онлайн

**Команды:**
- `/scoreboard` - Обновить скорборд
- `/scoreboard toggle` - Скрыть/показать (в разработке)

**API используемые методы:**
- `LXXVServer.createObjective()`
- `LXXVServer.removeObjective()`
- `LXXVServer.setScore()`

**Обновление:**
Скорборд обновляется каждые 2 секунды (40 тиков)

---

## 🔧 Конфигурация

### Изменение параметров

Каждый скрипт имеет конфигурационные переменные в начале файла:

**daily-rewards.js:**
```javascript
const DAILY_REWARD = 1000;    // Базовая награда
const STREAK_BONUS = 500;     // Бонус за серию
```

**auto-restart.js:**
```javascript
const RESTART_TIMES = ['04:00', '16:00'];  // Время рестартов
const WARNING_TIMES = [600, 300, 180, 60, 30, 10, 5];  // Предупреждения
```

**scoreboard-manager.js:**
```javascript
const UPDATE_INTERVAL = 40;  // Интервал обновления (тики)
```

---

## 📚 LXXVServer API

Все скрипты используют **LXXVServer API** - мост между Bukkit и JavaScript.

### Основные категории методов:

#### 🎮 Сервер
- `getOnlinePlayers()` - Список игроков онлайн
- `getMaxPlayers()` - Максимум игроков
- `broadcast(message)` - Сообщение всем
- `executeCommand(cmd)` - Выполнить команду

#### 👤 Игроки
- `getPlayer(name)` - Получить игрока
- `sendMessage(player, msg)` - Отправить сообщение
- `sendTitle(player, title, subtitle, fadeIn, stay, fadeOut)` - Титр
- `sendActionBar(player, msg)` - Action bar
- `kickPlayer(player, reason)` - Кикнуть
- `teleport(player, world, x, y, z)` - Телепорт
- `giveItem(player, material, amount)` - Выдать предмет

#### 🌍 Мир
- `setBlock(world, x, y, z, material)` - Установить блок
- `getBlock(world, x, y, z)` - Получить блок
- `spawnEntity(world, x, y, z, type)` - Заспавнить моба
- `setGameRule(world, rule, value)` - Установить правило
- `saveAllWorlds()` - Сохранить все миры

#### 🎵 Звуки и частицы
- `playSound(player, sound, volume, pitch)` - Воспроизвести звук
- `stopSound(player, sound)` - Остановить звук
- `spawnParticle(type, location, count, offsetX, offsetY, offsetZ, speed)` - Частицы

#### 📊 Scoreboard
- `createObjective(name, criteria, displayName)` - Создать objective
- `removeObjective(name)` - Удалить objective
- `setScore(objective, entry, value)` - Установить значение
- `getScore(objective, entry)` - Получить значение

#### 💰 Vault (экономика/права)
- `getBalance(player)` - Получить баланс
- `deposit(player, amount)` - Пополнить
- `withdraw(player, amount)` - Снять
- `hasPermission(player, perm)` - Проверить право
- `addPermission(player, perm)` - Добавить право

#### ⚡ События
- `on(event, callback)` - Подписаться на событие
- `emit(event, data)` - Вызвать событие

**Доступные события:**
- `playerJoin` - Вход игрока
- `playerQuit` - Выход игрока
- `playerDeath` - Смерть игрока
- `blockBreak` - Ломание блока
- `blockPlace` - Установка блока

#### 📅 Планировщик
- `runTask(callback)` - Выполнить в след. тик
- `runTaskLater(callback, delay)` - Выполнить через delay тиков
- `runTaskTimer(callback, delay, period)` - Повторять каждые period тиков
- `cancelTask(taskId)` - Отменить задачу

#### 🎯 Boss Bar
- `createBossBar(id, title, color, style)` - Создать boss bar
- `showBossBar(id, player)` - Показать игроку
- `removeBossBar(id, player)` - Убрать у игрока

#### 🛠 Команды
- `registerCommand(name, callback)` - Зарегистрировать команду

---

## 🐛 Отладка

### Просмотр логов

Логи скриптов выводятся в консоль сервера:

```
[Server] §a[Welcome] Скрипт приветствия загружен!
[Server] §a[DailyRewards] Система ежедневных наград загружена!
```

### Проверка ошибок

Если скрипт не работает:

1. Проверьте консоль на ошибки JavaScript
2. Убедитесь, что LXXV Common загружен
3. Проверьте права доступа к файлам
4. Убедитесь, что LoaderScript поддерживает V8/Javet

### Тестирование

```javascript
// Добавьте в начало скрипта для отладки
console.log('=== SCRIPT DEBUG ===');
console.log('LXXVServer:', typeof LXXVServer);
console.log('Online players:', LXXVServer.getOnlinePlayers());
```

---

## 📝 Создание своих скриптов

### Шаблон скрипта

```javascript
/**
 * My Custom Script - описание
 */

// Конфигурация
const CONFIG = {
    enabled: true,
    interval: 6000  // 5 минут
};

// Основная функция
function initialize() {
    console.log('§a[MyScript] Загружен!');
    
    // Ваш код здесь
}

// События
LXXVServer.on('playerJoin', (player) => {
    const playerName = player.getName();
    LXXVServer.sendMessage(playerName, '§aПривет!');
});

// Команды
LXXVServer.registerCommand('mycommand', (player, args) => {
    LXXVServer.sendMessage(player.getName(), '§eКоманда выполнена!');
});

// Запуск
initialize();
```

### Полезные советы

1. **Используйте константы** для настроек
2. **Логируйте** важные действия
3. **Проверяйте null** при работе с игроками
4. **Обрабатывайте ошибки** с try-catch
5. **Комментируйте код** для понимания

---

## 🔗 Ссылки

- **LXXVServer API**: `common/src/main/java/lxxv/shared/server/LXXVServer.java`
- **Paper API**: https://jd.papermc.io/paper/1.20/
- **Vault API**: https://github.com/MilkBowl/VaultAPI

---

## 📞 Поддержка

Если у вас возникли проблемы или вопросы:

1. Проверьте логи сервера
2. Прочитайте документацию LXXVServer
3. Создайте issue в репозитории проекта

---

**Версия:** 1.0.0  
**Автор:** LXXV Team  
**Лицензия:** MIT
