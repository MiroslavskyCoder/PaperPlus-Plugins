# Create2 - Mechanical Contraptions & Automation

**Оригинальная реализация концепций из [Creators-of-Create/Create](https://github.com/Creators-of-Create/Create) для Paper API**

## 📖 Описание

Create2 - это плагин для Paper сервера, реализующий систему механической автоматизации, вдохновлённую популярным модом **Create**. Плагин включает:

- ⚙️ **Кинематические сети** - система передачи вращательной энергии
- 🔧 **Механические компоненты** - валы, шестерни, двигатели
- ⚡ **Система стресса** - ограничение мощности сетей
- 🎯 **Расширяемая архитектура** - легко добавлять новые механизмы

## 🎯 Ключевые концепции

### Кинематическая сеть (Kinematic Network)

Система соединённых механических блоков, передающих **вращение (RPM)** и ограниченных **стрессом (SU - Stress Units)**.

```
[Водяное колесо] → [Вал] → [Шестерня] → [Пресс]
    512 SU          1 SU      2 SU        8 SU
```

### Вращение (Rotation)

- **RPM** (Rotations Per Minute) - скорость вращения
- Передаётся через валы и шестерни
- Может изменяться передаточными числами
- Максимум: 256 RPM (настраивается)

### Стресс (Stress)

- **Stress Impact** - потребление энергии (механизмы)
- **Stress Capacity** - генерация энергии (источники)
- Если `Stress > Capacity` → сеть **перегружена** → RPM падает

## 🔧 Компоненты

### Передача вращения

| Компонент | Блок | Стресс | Описание |
|-----------|------|--------|----------|
| **Shaft** (Вал) | Stripped Oak Log | 1.0 SU | Прямая передача вращения |
| **Cogwheel** (Малая шестерня) | Iron Block | 2.0 SU | Изменение передаточного числа |
| **Large Cogwheel** (Большая шестерня) | Gold Block | 2.0 SU | Передаточное число 2:1 |
| **Gearbox** (Коробка передач) | Diamond Block | 1.5 SU | Изменение направления |

### Источники энергии

| Компонент | Блок | RPM | Capacity | Описание |
|-----------|------|-----|----------|----------|
| **Hand Crank** | Lever | 32 | 0 | Ручное вращение |
| **Water Wheel** | Water Wheel | 16 | 512 SU | Водяная энергия |
| **Windmill** | End Rod | 8 | 1024 SU | Ветряная энергия |
| **Motor** | Beacon | 128 | 99999 SU | Креативный источник |

### Механизмы

| Компонент | Блок | Стресс | Описание |
|-----------|------|--------|----------|
| **Mechanical Press** | Anvil | 8.0 SU | Прессование предметов |
| **Mechanical Mixer** | Cauldron | 12.0 SU | Смешивание ингредиентов |
| **Millstone** | Grindstone | 4.0 SU | Измельчение |

## 🚀 Установка

1. Скачайте `Create2.jar` из релизов
2. Поместите в папку `plugins/`
3. Перезапустите сервер
4. Настройте `plugins/Create2/config.yml`

## 📝 Команды

```
/create2 stats          - Просмотр статистики
/create2 networks       - Список активных сетей
/create2 inspect        - Проверить компонент под ногами
/create2 debug <on|off> - Режим отладки
/create2 reload         - Перезагрузить конфиг
```

### Примеры использования

```bash
# Посмотреть общую статистику
/c2 stats

# Список всех кинематических сетей
/c2 networks

# Проверить компонент
/c2 inspect
```

## 🎮 Как использовать

### Создание простой сети

1. **Поставьте источник энергии**
   - Например, Water Wheel (водяное колесо)
   - Выдаёт 16 RPM, 512 SU

2. **Соедините валами**
   - Stripped Oak Log = Shaft (вал)
   - Передаёт вращение напрямую

3. **Добавьте механизм**
   - Anvil = Mechanical Press
   - Потребляет 8 SU

### Пример сети

```
Water Wheel (16 RPM, 512 SU)
    ↓
Shaft (передача)
    ↓
Cogwheel (изменение скорости)
    ↓
Press (8 SU) + Mixer (12 SU) = 20 SU
```

**Результат:** Сеть работает (20 SU < 512 SU capacity)

### Передаточные числа

```
Малая шестерня → Большая шестерня = 0.5x скорость (больше крутящего момента)
Большая шестерня → Малая шестерня = 2x скорость (меньше момента)
```

## ⚙️ Конфигурация

```yaml
# config.yml

kinematic:
  max-network-size: 1000        # Максимум блоков в сети
  update-tick-rate: 1           # Частота обновления (тики)
  max-stress: 10000.0           # Максимальный стресс на сеть
  stress-decay-rate: 0.1        # Скорость падения RPM при перегрузке

rotation:
  max-rpm: 256.0                # Максимальный RPM
  min-rpm: 0.1                  # Минимальный RPM (ниже = 0)
  transfer-efficiency: 0.98     # Эффективность передачи (98%)

power-sources:
  water-wheel:
    enabled: true
    rpm: 16.0
    stress-capacity: 512.0
```

## 🏗️ Архитектура

### Основные классы

```
com.webx.create2/
├── Create2Plugin.java              # Главный класс плагина
├── kinematic/
│   ├── KinematicNetwork.java       # Сеть механических блоков
│   ├── KinematicNode.java          # Один узел (блок)
│   ├── KinematicNodeType.java      # Типы компонентов
│   ├── KinematicNetworkManager.java # Менеджер всех сетей
│   └── RotationPropagator.java     # Распространение вращения
├── commands/
│   └── Create2Command.java         # Обработчик команд
└── listeners/
    └── BlockListener.java          # События блоков
```

### Алгоритмы

**Распространение вращения (BFS)**
```java
Queue<Node> queue;
queue.add(source);

while (!queue.isEmpty()) {
    Node current = queue.poll();
    double rpm = calculateOutputRpm(current);
    
    for (Node neighbor : getConnectedNodes(current)) {
        neighbor.setRpm(rpm * gearRatio);
        queue.add(neighbor);
    }
}
```

**Расчёт стресса**
```java
double totalStress = 0;
double totalCapacity = 0;

for (Node node : network.getNodes()) {
    if (node.isPowerSource()) {
        totalCapacity += node.getStressCapacity();
    }
    if (node.isPowerConsumer()) {
        totalStress += node.getStressImpact();
    }
}

boolean overstressed = totalStress > totalCapacity;
```

**Разделение сетей (при удалении блока)**
```java
Set<Set<Node>> connectedComponents = findConnectedComponents(network);

for (Set<Node> component : connectedComponents) {
    KinematicNetwork newNetwork = new KinematicNetwork();
    component.forEach(newNetwork::addNode);
}
```

## 🔬 Технические детали

### Производительность

- **Асинхронные вычисления**: Расчёты сетей могут быть async
- **Кэширование**: Сети кэшируются для быстрого доступа
- **Оптимизация**: BFS с visited set предотвращает циклы

### Ограничения

- Максимум блоков в сети: 1000 (настраивается)
- Максимум RPM: 256
- Максимум стресса: 10000 SU

### Зависимости

- **Paper API** 1.20.4+
- **JOML** 1.10.5 (векторная математика)

## 📊 Сравнение с Create Mod

| Функция | Create (Forge/Fabric) | Create2 (Paper) | Статус |
|---------|----------------------|-----------------|--------|
| Кинематические сети | ✅ | ✅ | Полная поддержка |
| Система стресса | ✅ | ✅ | Полная поддержка |
| Передаточные числа | ✅ | ✅ | Базовая поддержка |
| Валы и шестерни | ✅ | ✅ | Базовая поддержка |
| Источники энергии | ✅ | ✅ | 4 типа |
| Механические устройства | ✅ | ⚠️ | 3 базовых (расширяется) |
| Конвейеры | ✅ | ❌ | Планируется |
| Трубы и жидкости | ✅ | ❌ | Планируется |
| Кастомные модели | ✅ | ❌ | Ограничения Paper API |
| Анимации | ✅ | ❌ | Ограничения Paper API |

**Легенда:** ✅ Реализовано | ⚠️ Частично | ❌ Не реализовано

## 🔮 Планы развития

### v1.1.0
- [ ] Конвейерные ленты (Belt)
- [ ] Воронки (Funnel)
- [ ] Деплойер (Deployer)

### v1.2.0
- [ ] Система жидкостей (Fluid Handling)
- [ ] Трубы и насосы (Pipes & Pumps)
- [ ] Механическая дрель (Drill)

### v1.3.0
- [ ] Автоматическая ферма (Harvester)
- [ ] Система контрактов (Contraptions)
- [ ] Летающие машины (Flying Machines)

## 🐛 Известные проблемы

1. **Нет кастомных моделей** - используются стандартные блоки Minecraft
2. **Нет анимаций** - Paper API не поддерживает кастомные анимации
3. **Упрощённая физика** - оригинальный Create имеет более сложную систему

## 🤝 Вклад в проект

Приветствуются:
- 🐛 Баг-репорты
- 💡 Идеи для новых механизмов
- 📝 Улучшения документации
- 🔧 Pull requests

## 📄 Лицензия

MIT License - свободное использование и модификация

## 🙏 Благодарности

- **Creators-of-Create** - за оригинальный мод Create
- **Paper Team** - за отличный серверный API
- **JOML** - за математическую библиотеку

## 📞 Поддержка

- GitHub Issues: [создать issue](#)
- Discord: (добавьте свой сервер)
- Wiki: [документация](#)

---

**Важно:** Это **оригинальная реализация концепций** Create mod для Paper API. Код написан с нуля, не копирует оригинальный Create, но вдохновлён его дизайном и механиками.

**Made with ❤️ for Minecraft server communities**
