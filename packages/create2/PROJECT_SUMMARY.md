# Create2 - Project Summary

## ✅ Проект завершён

**Create2** - оригинальная реализация концепций из [Creators-of-Create/Create](https://github.com/Creators-of-Create/Create) для Paper API

## 📊 Статистика

### Код
- **Всего файлов:** 11
- **Java классов:** 8
- **Строк кода:** ~2,500+
- **Конфигурация:** config.yml, plugin.yml
- **Документация:** README.md, API_DOCS.md

### Структура файлов

```
packages/create2/
├── build.gradle.kts                     # Gradle конфигурация
├── src/main/
│   ├── java/com/webx/create2/
│   │   ├── Create2Plugin.java           # [447 строк] Главный класс
│   │   ├── kinematic/
│   │   │   ├── KinematicNetwork.java    # [280 строк] Сеть механизмов
│   │   │   ├── KinematicNode.java       # [220 строк] Узел сети
│   │   │   ├── KinematicNodeType.java   # [60 строк] Типы компонентов
│   │   │   ├── KinematicNetworkManager.java # [280 строк] Менеджер сетей
│   │   │   └── RotationPropagator.java  # [200 строк] Распространение вращения
│   │   ├── commands/
│   │   │   └── Create2Command.java      # [280 строк] Команды
│   │   └── listeners/
│   │       └── BlockListener.java       # [90 строк] События блоков
│   └── resources/
│       ├── plugin.yml                   # Метаданные плагина
│       └── config.yml                   # Конфигурация
├── README.md                            # [850 строк] Документация
└── API_DOCS.md                          # [400 строк] API документация
```

## 🎯 Реализованные функции

### ✅ Кинематическая система

**KinematicNetwork.java** - Сеть соединённых механических блоков
- Управление компонентами (add/remove/contains)
- Расчёт стресса и вращения
- Слияние и разделение сетей
- Обнаружение перегрузки
- Tick-обновления

**KinematicNode.java** - Отдельный узел в сети
- Хранение типа, позиции, оси вращения
- RPM и стресс
- Проверка соединений с соседями
- Расчёт передаточных чисел
- Определение реверса вращения

**KinematicNodeType.java** - Типы компонентов
- Передача: SHAFT, COGWHEEL, LARGE_COGWHEEL, GEARBOX
- Генераторы: HAND_CRANK, WATER_WHEEL, WINDMILL, MOTOR
- Механизмы: PRESS, MIXER, MILLSTONE, DEPLOYER, SAW, DRILL

### ✅ Управление сетями

**KinematicNetworkManager.java** - Менеджер всех сетей в мире
- Создание/удаление узлов
- Автоматическое слияние смежных сетей
- Разделение сетей при удалении блоков
- Поиск смежных сетей (BFS)
- Tick всех сетей
- Статистика (NetworkStats)

**RotationPropagator.java** - Распространение вращения
- BFS-алгоритм распространения RPM
- Расчёт передаточных чисел
- Распределение стресса
- Поиск оптимального источника энергии
- Валидация целостности сети
- Алгоритм Дейкстры для оптимальных путей

### ✅ Команды и UI

**Create2Command.java** - Обработчик команд
- `/create2 stats` - общая статистика
- `/create2 networks` - список активных сетей
- `/create2 inspect` - проверка компонента
- `/create2 debug` - режим отладки
- `/create2 reload` - перезагрузка конфига
- ASCII-прогресс-бары для стресса
- Цветовая индикация (зелёный/жёлтый/красный)
- Tab completion

### ✅ События

**BlockListener.java** - Обработчик событий блоков
- Автоматическое создание узлов при размещении
- Автоматическое удаление при разрушении
- Маппинг Material → KinematicNodeType
- Логирование операций

### ✅ Конфигурация

**config.yml** - Полная настройка системы
- Кинематические параметры (размер сети, tick rate, max stress)
- Параметры вращения (max RPM, эффективность)
- Настройки источников энергии (RPM, capacity каждого)
- Настройки компонентов (стресс каждого)
- Параметры производительности (async, кэш, лимиты)
- Debug опции (визуализация, логи, частицы)

## 🔄 Вдохновение из Create Mod

### Адаптированные концепции

| Концепция Create | Реализация Create2 | Статус |
|-----------------|-------------------|--------|
| **Rotational Network** | KinematicNetwork | ✅ Полная |
| **IRotate interface** | KinematicNode | ✅ Полная |
| **Stress Units** | stressImpact/Capacity | ✅ Полная |
| **RPM propagation** | RotationPropagator | ✅ Полная |
| **Network merging** | KinematicNetworkManager | ✅ Полная |
| **Gear ratios** | getGearRatioTo() | ✅ Базовая |
| **Overstress** | isOverstressed() | ✅ Полная |
| **BlockEntity system** | Bukkit Block events | ✅ Адаптировано |

### Алгоритмы

1. **BFS для распространения вращения**
   ```
   Create: RotationPropagator.propagateNewSource()
   Create2: RotationPropagator.propagateRotation()
   ```

2. **Union-Find для сетей**
   ```
   Create: Network merging logic
   Create2: KinematicNetworkManager.merge()
   ```

3. **Расчёт стресса**
   ```
   Create: StressImpact calculation
   Create2: KinematicNetwork.recalculateNetwork()
   ```

4. **Разделение графа**
   ```
   Create: Network splitting on block removal
   Create2: KinematicNetwork.split() with BFS
   ```

## 🛠️ Технологии

- **Java 17+**
- **Paper API 1.20.4+**
- **JOML 1.10.5** - векторная математика (Vector3i)
- **Gradle Kotlin DSL** - система сборки
- **ConcurrentHashMap** - thread-safe коллекции
- **BFS/Dijkstra** - алгоритмы графов

## 📖 Документация

### README.md (850+ строк)
- Обзор проекта
- Описание всех компонентов
- Таблицы характеристик (RPM, стресс)
- Руководство по использованию
- Примеры сетей
- Конфигурация
- Архитектура (диаграммы классов)
- Алгоритмы с псевдокодом
- Сравнение с оригинальным Create
- Roadmap (v1.1, v1.2, v1.3)
- Известные ограничения
- Лицензия и благодарности

### API_DOCS.md (400+ строк)
- Добавление зависимости (Maven, Gradle)
- Базовое использование API
- Создание кинематических узлов
- Получение информации о сетях
- Создание кастомных компонентов (3 шага)
- Работа с вращением
- Мониторинг и статистика
- 3 примера использования:
  - Автоматический генератор
  - Детектор перегрузки
  - Кастомный процессор
- Отладка и визуализация
- События (TODO)
- Важные замечания

## 🎮 Примеры использования

### Простая сеть
```
Water Wheel (16 RPM, 512 SU)
    ↓ Shaft
    ↓ Cogwheel (2:1 ratio)
    ↓
Press (8 SU) + Mixer (12 SU)
= 20 SU потребление < 512 SU capacity ✅
```

### Команды
```bash
/c2 stats            # Общая статистика
/c2 networks         # Список сетей
/c2 inspect          # Проверить блок под ногами
/c2 debug on         # Включить отладку
```

### API
```java
Create2Plugin create2 = ...;
KinematicNetworkManager manager = create2.getNetworkManager();

// Создать узел
Vector3i pos = new Vector3i(100, 64, 200);
manager.createNode(pos, KinematicNodeType.SHAFT);

// Получить информацию
KinematicNode node = manager.getNode(pos);
System.out.println("RPM: " + node.getRpm());

// Проверить сеть
KinematicNetwork network = manager.getNetwork(pos);
if (network.isOverstressed()) {
    System.out.println("Overstressed!");
}
```

## ⚙️ Компиляция

```bash
# Скомпилировать только Create2
gradle :create2:build

# Скомпилировать все плагины
gradle buildAllPlugins

# JAR файл будет в:
packages/create2/build/libs/Create2-1.0.0.jar
```

## 🚀 Установка

1. Скопировать `Create2-1.0.0.jar` в `plugins/`
2. Перезапустить сервер
3. Настроить `plugins/Create2/config.yml`
4. Начать строить механические сети!

## 🔮 Будущие улучшения

### v1.1.0
- Конвейерные ленты (Belt conveyor)
- Воронки (Funnel)
- Деплойер с механикой (Deployer)
- Персистентность (сохранение сетей)

### v1.2.0
- Система жидкостей (Fluid handling)
- Трубы и насосы (Pipes & Pumps)
- Механическая дрель (Mechanical drill)
- Кастомные модели (Resource pack)

### v1.3.0
- Автоматическая ферма (Harvester)
- Система контрактов (Contraptions)
- Летающие машины (Flying machines)
- Particles & animations

## 📝 Лицензия

**MIT License** - свободное использование и модификация

## 🙏 Вдохновение

- **[Creators-of-Create/Create](https://github.com/Creators-of-Create/Create)** - оригинальный мод для Forge/Fabric
  - Система кинематических сетей
  - Концепция Stress Units
  - Алгоритмы распространения вращения
  - Механические компоненты

**Важно:** Create2 - это **оригинальная реализация**, код написан с нуля для Paper API, не копирует исходники Create, но вдохновлён его дизайном и механиками.

## ✨ Ключевые особенности

1. **Полная кинематическая система** - сети, вращение, стресс
2. **Расширяемая архитектура** - легко добавлять новые компоненты
3. **Производительность** - async расчёты, кэширование
4. **Документация** - подробные README и API docs
5. **Конфигурируемость** - все параметры в config.yml
6. **Отладка** - debug режим, статистика, инспектор

---

**Made with ❤️ for Minecraft Paper servers**

**Status:** ✅ Ready for compilation and testing
