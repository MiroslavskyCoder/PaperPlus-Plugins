# 💀 DeathMark - Сундуки смерти

Плагин для сохранения вещей игрока в защищенном сундуке при смерти с 3D текстом.

## ✨ Возможности

- 📦 Автоматическое создание сундука на месте смерти
- 👤 3D текст над сундуком с именем игрока (☠ ИмяИгрока ☠)
- 🔒 Защита от других игроков
- ♾️ Вещи хранятся вечно (не исчезают)
- 🎯 Умный поиск безопасного места для сундука
- 📍 Уведомление с координатами сундука
- 🎨 Настраиваемые сообщения

## 🎮 Как работает

1. Игрок умирает → вещи собираются
2. Ищется безопасное место (10 позиций)
3. Создаётся сундук с вещами
4. Над сундуком появляется 3D текст с именем
5. Игрок получает сообщение с координатами
6. Только владелец может открыть/сломать сундук

## ⚙️ Конфигурация

```yaml
# Whether to enable death chests
enabled: true

# Whether to protect chests from other players
protected: true

# How long (in seconds) until chest expires (0 = never)
expire-seconds: 0

# Whether to send a message to player about chest location
notify-player: true
```

### Настройка сообщений

```yaml
messages:
  chest-created: "&aВаши вещи сохранены в сундуке на &e{x}, {y}, {z}"
  chest-opened: "&aВы открыли свой сундук со смертью"
  chest-protected: "&cЭтот сундук принадлежит другому игроку"
  chest-expired: "&7Ваш сундук со смертью исчез через {time} секунд"
```

### Плейсхолдеры

- `{x}`, `{y}`, `{z}` - координаты сундука
- `{time}` - время до исчезновения (если включено)

## 🎯 Умный поиск места

Плагин пытается разместить сундук в таком порядке:
1. Точная позиция смерти
2. На 1 блок выше
3. В радиусе 1 блока вокруг (8 позиций)

Если все позиции заняты → вещи выпадают обычным способом.

## 📦 Установка

1. Скачайте `death-mark-0.1.0.jar`
2. Поместите в папку `plugins/` сервера
3. Перезапустите сервер
4. Настройте `plugins/DeathMark/config.yml`

## 🔧 Требования

- Paper/Spigot 1.20.4+
- Java 17+

## 💡 Примеры использования

### Обычная смерть
```
Игрок умирает на координатах 100, 64, -200
→ Создаётся сундук
→ Над сундуком текст: ☠ ИмяИгрока ☠ (золотой цвет)
→ Сообщение: "Ваши вещи сохранены в сундуке на 100, 64, -200"
```

### Попытка открыть чужой сундук
```
ДругойИгрок пытается открыть → ❌ "Этот сундук принадлежит другому игроку"
Владелец открывает → ✅ "Вы открыли свой сундук со смертью"
```

### Ломание сундука
```
ДругойИгрок пытается сломать → ❌ Отменено, сообщение о защите
Владелец ломает → ✅ Сундук ломается, вещи выпадают, текст удаляется
```

## 🔒 Система защиты

### Защита от других игроков
- ❌ Невозможно открыть чужой сундук
- ❌ Невозможно сломать чужой сундук
- ✅ Только владелец имеет доступ

### 3D Текст
- 📍 Плавает над сундуком на высоте 1.5 блока
- 👁️ Виден всем игрокам
- 🎨 Золотой цвет с черепами: `☠ ИмяИгрока ☠`
- 🔄 Автоматически удаляется с сундуком

## 🎨 Визуальные особенности

### Формат текста
```
☠ ИмяИгрока ☠
```
- Цвет: Золотой (`ChatColor.GOLD`)
- Имя: Жёлтое (`ChatColor.YELLOW`)
- Всегда видимый (CustomNameVisible: true)
- Невидимый стенд (только текст)

## 📊 Технические детали

### ArmorStand параметры
```java
textStand.setVisible(false);        // Сам стенд невидим
textStand.setGravity(false);        // Не падает
textStand.setInvulnerable(true);    // Неуязвим
textStand.setMarker(true);          // Маркер (не блокирует)
textStand.setCustomNameVisible(true); // Имя видно
```

### Хранение данных
```java
Map<Location, UUID> chests           // Владельцы сундуков
Map<Location, ArmorStand> textStands // Текстовые стенды
```

## ⚡ Оптимизация

- Легковесный (9.7 KB JAR)
- Минимальное использование памяти
- Нет таймеров (вещи не исчезают)
- Эффективная очистка при выключении

## 🐛 Известные проблемы

Нет известных проблем. Если нашли баг - сообщите разработчикам!

## 📝 Версия

**0.1.0** - Первый релиз
- Создание сундуков смерти
- 3D текст с именем игрока
- Защита от других игроков
- Вечное хранение вещей
- Умный поиск места для сундука
- Уведомления с координатами

## 🔄 История изменений

### v0.1.0
- ✨ Добавлен 3D текст над сундуком
- ✨ Изменено время хранения на бесконечное (expire-seconds: 0)
- ✨ Добавлена защита от других игроков
- ✨ Умный алгоритм поиска безопасного места
