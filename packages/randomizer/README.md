# Randomizer Plugin

Плагин для случайных телепортаций, выдачи предметов и генерации чисел.

## Возможности

- 🎯 **Случайная телепортация** - телепорт на безопасную поверхность без монстров поблизости
- 🪵 **Случайные предметы** - выдача случайных видов дерева и палок
- 🎲 **Случайные числа** - генерация случайных чисел от 1 до 1000
- ⏱️ **Кулдаун** - интервал 3 минуты между получением предметов

## Команды

| Команда | Описание | Пример |
|---------|----------|--------|
| `/randomizer` | Показать меню с доступными командами | `/randomizer` |
| `/randomtp` | Телепортироваться в случайное безопасное место | `/randomtp` |
| `/randomitem` | Получить случайный предмет (дерево/палки) | `/randomitem` |
| `/randomnumber` | Получить случайное число от 1 до 1000 | `/randomnumber` |

## Особенности

### Случайная телепортация
- Телепортирует игрока в радиусе 1000 блоков от текущей позиции
- Проверяет безопасность места (твёрдая поверхность, нет блоков над головой)
- Проверяет отсутствие монстров в радиусе 10 блоков
- Не телепортирует в воду или лаву

### Случайные предметы
- Выдаются только виды дерева и палки:
  - Oak, Spruce, Birch, Jungle, Acacia, Dark Oak, Mangrove, Cherry (брёвна и доски)
  - Палки
- Количество: от 1 до 16 предметов
- Кулдаун: 3 минуты между получением предметов

### Случайные числа
- Генерирует число от 1 до 1000
- Без кулдауна

## Конфигурация

```yaml
# Время кулдауна в минутах между получением предметов
cooldown-minutes: 3

# Максимальное расстояние телепортации от текущей позиции
max-teleport-distance: 1000

# Радиус проверки монстров вокруг точки телепортации
monster-check-radius: 10

# Сообщения
messages:
  cooldown: "&cВы должны подождать еще %time% секунд!"
  item-received: "&aВы получили &f%amount%x %item%&a!"
  teleport-success: "&aВы телепортированы на случайное место!"
  teleport-coords: "&7Координаты: &fX: %x% Y: %y% Z: %z%"
  teleport-failed: "&cНе удалось найти безопасное место для телепортации!"
  random-number: "&aВаше случайное число: &f%number%"
```

## Примеры использования

1. **Телепортация в случайное место:**
   ```
   /randomtp
   ```
   Результат: Телепортация на безопасную поверхность с координатами

2. **Получение случайных предметов:**
   ```
   /randomitem
   ```
   Результат: Получение 1-16 случайных брёвен, досок или палок

3. **Генерация случайного числа:**
   ```
   /randomnumber
   ```
   Результат: Случайное число от 1 до 1000

## Зависимости

- Paper API 1.20.4+
- Java 21+

## Установка

1. Скопируйте `randomizer-1.0.0.jar` в папку `plugins/`
2. Перезапустите сервер
3. Настройте `plugins/Randomizer/config.yml` при необходимости

## Разработка

Структура проекта:
```
randomizer/
├── src/main/java/com/webx/randomizer/
│   ├── RandomizerPlugin.java          # Главный класс плагина
│   ├── commands/
│   │   └── RandomCommand.java         # Обработчик команд
│   ├── managers/
│   │   └── RandomizerManager.java     # Логика рандомизации
│   └── listeners/
│       └── RandomRewardListener.java  # Обработчик событий
└── src/main/resources/
    ├── plugin.yml
    └── config.yml
```

## API для разработчиков

```java
// Получить экземпляр плагина
RandomizerPlugin plugin = RandomizerPlugin.getInstance();
RandomizerManager manager = plugin.getRandomizerManager();

// Телепортировать игрока
manager.randomTeleport(player);

// Выдать случайный предмет (с проверкой кулдауна)
manager.giveRandomItem(player);

// Получить случайное число
int number = manager.getRandomNumber();

// Проверить кулдаун
boolean canUse = manager.checkCooldown(player);
```

## Лицензия

MIT License
