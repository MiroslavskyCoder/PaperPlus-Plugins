# HDPhysicsSound

Оригинальная реализация идей из [henkelmax/sound-physics-remastered](https://github.com/henkelmax/sound-physics-remastered) для Paper API.

## Возможности
- 🔊 Затухание звука с расстоянием (air absorption)
- 🧱 Окклюзия через блоки (ray tracing)
- 🌀 Простейший реверб (wet tail)
- ⚙️ Настраиваемые параметры в `config.yml`
- 🛠️ Команда `/hdps` для тестов, отладки и перезагрузки

## Установка
1) Скомпилировать: `gradle :hdphysicssound:build`
2) Скопировать `packages/hdphysicssound/build/libs/HDPhysicsSound-1.0.0.jar` в `plugins/`
3) Перезапустить сервер

## Команды
- `/hdps test [vol] [pitch]` — проиграть тестовый звук с физикой
- `/hdps reload` — перезагрузить конфиг
- `/hdps debug` — вывести текущие параметры

## Конфиг (основное)
```yaml
physics:
  max-distance: 48.0
  air-absorption-per-meter: 0.02
  occlusion-steps: 6
  occlusion-penalty: 0.12
  min-occlusion-volume: 0.15

reverb:
  enabled: true
  room-scale: 0.65
  decay: 0.45
  wet-gain: 0.25
```

## Как это работает
1. Берём игроков в радиусе `max-distance`
2. Считаем потерю громкости от воздуха: `1 - distance * air_absorption`
3. Считаем окклюзию: несколько ray-trace по пути; за каждый блок штраф `occlusion-penalty`
4. Ограничиваем минимумом `min-occlusion-volume`
5. Проигрываем звук с рассчитанным volume; опционально ставим отложенный реверб

## Ограничения
- Используются стандартные звуки Paper API (без кастомных ресурсов)
- Простая модель реверба (одна задержка)
- Нет HRTF/стерео панорамирования — только громкость и задержка

## Расширение
- Добавьте свои типы звуков, вызывая `SoundPhysicsEngine.broadcastPhysicalSound(...)`
- Подключайте к своим событиям (шаги, выстрелы, кастомные умения)

## Лицензия
MIT. Код написан с нуля, без копирования исходников.
