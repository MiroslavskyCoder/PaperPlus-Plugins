# Quick Start Guide - ModernFix

## 🚀 Быстрый старт (5 минут)

### 1. Сборка проекта

```bash
cd my-polyglot-project
gradle :modernfix:build
```

JAR файл: `packages/modernfix/build/libs/modernfix.jar`

### 2. Установка

```bash
# Копируем в папку plugins
cp packages/modernfix/build/libs/modernfix.jar /path/to/server/plugins/

# Перезапускаем сервер
# Конфиг создастся автоматически в plugins/ModernFix/config.yml
```

### 3. Первая настройка

**config.yml:**
```yaml
entity-optimization:
  enabled: true
  max-age-ticks: 6000      # 5 минут

chunk-optimization:
  enabled: true

memory-optimization:
  enabled: true
  threshold-percent: 85    # GC при >85%

tick-optimization:
  enabled: true
```

### 4. Проверка работы

```
/modernfix stats
```

Вы должны увидеть:
```
╔═══════════════════════════════════╗
║   ModernFix Статистика            ║
╠═══════════════════════════════════╣
║ TPS: 20.00                        ║
║ Память: 2048/4096 МБ (50.0%)     ║
...
```

## 📋 Основные команды

| Команда | Описание | Право |
|---------|----------|-------|
| `/modernfix stats` | Статистика сервера | - |
| `/modernfix metrics` | Метрики производительности | - |
| `/modernfix reload` | Перезагрузка конфига | modernfix.reload |
| `/modernfix gc` | Принудительный GC | modernfix.gc |
| `/modernfix optimize` | Принудительная оптимизация | modernfix.optimize |

Алиасы: `/mfix`, `/optimize`

## 🔧 Права (permissions)

Добавьте в `permissions.yml` или через LuckPerms:

```yaml
modernfix.reload: true      # Перезагрузка конфига
modernfix.gc: true          # Запуск GC
modernfix.optimize: true    # Принудительная оптимизация
```

Или всё сразу:
```yaml
groups:
  admin:
    permissions:
      - modernfix.*
```

## 📊 Мониторинг

### Базовая статистика
```
/modernfix stats
```

### Детальные метрики
```
/modernfix metrics
```

Показывает:
- Среднее время выполнения операций
- Min/Max время
- Количество обработанных элементов
- Items/second rate

### Логи

**Console logs:**
```
[ModernFix] Запущена оптимизация сущностей (каждые 30 секунд)
[ModernFix] Запущена оптимизация чанков (каждые 60 секунд)
[ModernFix] Запущена оптимизация памяти (каждые 120 секунд)
```

**Fine logs** (debug mode):
```
[ModernFix] Оптимизация сущностей: удалено 45 из 1234 за 12 мс
[ModernFix] Мир world: кэшировано 567 чанков, очищено 23 ссылок за 8 мс
```

## 🎯 Типичные сценарии

### Сценарий 1: Высокое использование памяти

**Проблема:** Сервер использует >85% памяти

**Решение:**
```
/modernfix gc
```

Или уменьшите порог в конфиге:
```yaml
memory-optimization:
  threshold-percent: 75    # GC при >75% вместо 85%
```

### Сценарий 2: Много старых дропов

**Проблема:** Игроки жалуются на лаги от item entities

**Решение:**
```yaml
entity-optimization:
  enabled: true
  max-age-ticks: 3000    # Уменьшить с 6000 до 3000 (2.5 минут)
```

```
/modernfix reload
```

### Сценарий 3: Проверка эффективности

**Задача:** Узнать, работает ли оптимизация

**Действия:**
1. `/modernfix stats` - посмотреть общую статистику
2. `/modernfix metrics` - посмотреть метрики операций
3. Проверить console logs

**Что смотреть:**
- `Всего оптимизаций:` должно расти
- `Удалено сущностей:` должно быть >0
- `Память освобождено:` должно увеличиваться
- `Hit rate` в cache должен быть >70%

## 🐛 Troubleshooting

### Плагин не загружается

**Проверьте:**
1. Java версия >= 17
   ```bash
   java -version
   ```
2. Paper версия >= 1.20.4
3. Нет конфликтующих плагинов

### Оптимизации не работают

**Проверьте config.yml:**
```yaml
entity-optimization:
  enabled: true    # Должно быть true
```

**Перезагрузите:**
```
/modernfix reload
```

### Ошибки в консоли

**"Uncaught exception":**
- Проверьте версию Paper
- Отключите конфликтующие плагины
- Создайте Issue с полным логом

## 📈 Оптимальные настройки

### Для маленького сервера (1-20 игроков)
```yaml
entity-optimization:
  max-age-ticks: 6000    # 5 минут
memory-optimization:
  threshold-percent: 85   # Высокий порог
```

### Для среднего сервера (20-50 игроков)
```yaml
entity-optimization:
  max-age-ticks: 4000    # 3.3 минуты
memory-optimization:
  threshold-percent: 80   # Средний порог
```

### Для большого сервера (50+ игроков)
```yaml
entity-optimization:
  max-age-ticks: 3000    # 2.5 минуты
memory-optimization:
  threshold-percent: 75   # Низкий порог (чаще GC)
```

## 🔗 Полезные ссылки

- [README.md](README.md) - Полная документация
- [IMPLEMENTATION_SUMMARY.md](IMPLEMENTATION_SUMMARY.md) - Технические детали
- [config.yml](src/main/resources/config.yml) - Конфигурация по умолчанию

## 💡 Советы

1. **Не злоупотребляйте `/modernfix gc`** - GC сам работает хорошо
2. **Мониторьте метрики** - используйте `/modernfix metrics` для анализа
3. **Настройте под свой сервер** - не все дефолты подходят всем
4. **Проверяйте TPS** - основной показатель производительности
5. **Используйте права** - дайте админам modernfix.*

---

**Готово! Ваш сервер оптимизирован! 🚀**
