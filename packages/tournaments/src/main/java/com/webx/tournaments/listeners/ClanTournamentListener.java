package com.webx.tournaments.listeners;

import org.bukkit.event.Listener;

public class ClanTournamentListener implements Listener {
    // TODO: Listen for tournament events involving clans (entry, win, reward)
}
