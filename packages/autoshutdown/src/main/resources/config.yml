# AutoShutdown Configuration

# Enable auto shutdown
enabled: true

# Time in minutes before shutdown (after last player leaves)
timeout: 10

# Warning messages (minutes before shutdown)
warnings:
  - 5
  - 3
  - 1

# Shutdown message
shutdown-message: "§cServer shutting down due to inactivity..."
