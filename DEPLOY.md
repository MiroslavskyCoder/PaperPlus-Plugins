# Инструкция по сборке и деплою

## Проблема с кэшированием файлов

При сборке плагина веб-файлы упаковываются внутрь JAR файла. Если не очищать старые файлы, они могут кэшироваться.

### Типичная ошибка 404

Если вы видите в консоли браузера:
```
Failed to load resource: the server responded with a status of 404 (Not Found)
e6ff975709dfa1a4.css:1
2cccc214a8373525.js:1
```

**Причина**: Браузер кэшировал старую HTML страницу со ссылками на старые CSS/JS файлы, которых уже нет в новой сборке.

**Решение**:
1. **На сервере**: Всегда используйте `gradlew clean build` перед деплоем
2. **В браузере**: Очистите кэш (Ctrl+Shift+R или Hard Reload в DevTools)
3. **Автоматически**: Плагин теперь отправляет правильные заголовки Cache-Control:
   - HTML файлы: `no-cache` (всегда загружаются свежие)
   - CSS/JS/шрифты: `max-age=31536000` (кэшируются 1 год, т.к. имена с хешами меняются)

### Страница без стилей (только HTML)

Если страница загружается но без стилей CSS - видны только текст и кнопки без оформления:

**Причина**: 
- Старый JAR файл на сервере (CSS файлы из предыдущей сборки)
- JAR не был заменён или сервер не перезапущен
- Файлы не скопировались в JAR (забыли `clean`)

**Проверка в логах сервера**:
```
✅ [HTTP] GET /_next/static/chunks/9f82a1c07477eb5a.css - 200
```
Если видите `❌ 404` - значит файлы не в JAR.

**Решение**:
1. Проверьте что используете правильный JAR: `ls -lh plugins/webx-dashboard-1.0.0.jar`
2. Убедитесь что собрали с `clean`: `./gradlew clean build`
3. Проверьте содержимое JAR: `jar -tf webx-dashboard-1.0.0.jar | grep "\.css$"`
4. Полностью остановите сервер перед заменой JAR
5. Очистите кэш браузера (Ctrl+Shift+R)

## Правильная процедура сборки

### 1. Полная пересборка (рекомендуется)

```bash
# Windows
gradlew.bat clean build

# Linux/Mac
./gradlew clean build
```

Команда `clean` удалит:
- Старые скомпилированные классы
- Старые веб-файлы из `src/main/resources/web`
- Старые JAR файлы из `build/libs`

### 2. Быстрая пересборка (если меняли только фронтенд)

```bash
# Windows
gradlew.bat cleanFrontend copyFrontend jar

# Linux/Mac
./gradlew cleanFrontend copyFrontend jar
```

## Деплой на Ubuntu сервер

### Вариант 1: Остановка сервера

```bash
# 1. Остановите Minecraft сервер
screen -r minecraft
# Нажмите Ctrl+C или введите "stop"

# 2. Удалите старый плагин
rm /path/to/server/plugins/webx-dashboard-1.0.0.jar

# 3. Скопируйте новый плагин
cp webx-dashboard-1.0.0.jar /path/to/server/plugins/

# 4. Запустите сервер
cd /path/to/server
java -jar paper.jar
```

### Вариант 2: Hot reload (если поддерживается)

```bash
# 1. Скопируйте новый JAR
cp webx-dashboard-1.0.0.jar /path/to/server/plugins/

# 2. Используйте PlugMan или подобный плагин
/plugman reload WebxDashboard
```

### Вариант 3: Через FTP/SCP

```bash
# Локально собираем
gradlew.bat clean build

# Копируем на сервер
scp webx-dashboard/build/libs/webx-dashboard-1.0.0.jar user@server:/path/to/plugins/

# На сервере
ssh user@server
cd /path/to/server
screen -r minecraft
# В консоли Minecraft:
/reload confirm
```

## Проверка обновления

После деплоя проверьте:

1. **Версия файлов в браузере**: Откройте DevTools (F12) → Network → Hard Reload (Ctrl+Shift+R)
2. **Логи сервера**: Проверьте что плагин загрузился
3. **API**: Проверьте что новые эндпоинты доступны

## Если веб-файлы всё равно старые

### На сервере проверьте JAR

```bash
# Просмотреть содержимое JAR
jar -tf webx-dashboard-1.0.0.jar | grep web/

# Извлечь веб-файлы для проверки
jar -xf webx-dashboard-1.0.0.jar web/
ls -la web/
```

### Очистка кэша браузера

Иногда проблема в кэше браузера:

1. Откройте DevTools (F12)
2. Кликните правой кнопкой на кнопку обновления
3. Выберите "Empty Cache and Hard Reload"

Или очистите кэш через настройки браузера.

### Очистка кэша Javalin

Если Javalin кэширует статические файлы, можно:

1. Остановить сервер
2. Удалить папку `cache` (если есть)
3. Перезапустить сервер

## Структура сборки

```
Процесс сборки:
1. :frontend-panel:bunBuild → собирает Next.js в /frontend-panel/out
2. :webx-dashboard:cleanFrontend → удаляет /src/main/resources/web
3. :webx-dashboard:copyFrontend → копирует out → resources/web
4. :webx-dashboard:compileJava → компилирует Java классы
5. :webx-dashboard:processResources → копирует resources в build
6. :webx-dashboard:jar → упаковывает всё в JAR

JAR структура:
webx-dashboard-1.0.0.jar
├── com/webx/... (Java классы)
├── web/ (статические файлы фронтенда)
│   ├── index.html
│   ├── _next/
│   └── ...
├── config.yml
└── plugin.yml
```

## Автоматизация

Создайте скрипт для автоматического деплоя:

```bash
#!/bin/bash
# deploy.sh

echo "🔨 Сборка проекта..."
./gradlew clean build

if [ $? -ne 0 ]; then
    echo "❌ Ошибка сборки!"
    exit 1
fi

echo "📦 Копирование на сервер..."
scp webx-dashboard/build/libs/webx-dashboard-1.0.0.jar user@server:/path/to/plugins/

echo "🔄 Перезагрузка плагина..."
ssh user@server "screen -S minecraft -X stuff 'plugman reload WebxDashboard\n'"

echo "✅ Деплой завершён!"
```

Сделайте скрипт исполняемым:
```bash
chmod +x deploy.sh
./deploy.sh
```
