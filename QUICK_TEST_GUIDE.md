# 🚀 Быстрый старт - Тестирование интеграции плагинов

## ✅ Что уже реализовано

### 1. 💰 **Начисление монет за убийство монстров**
- Плагин `Combat` → интеграция с `Economy`
- +1 монета за каждое убийство монстра
- Сохранение в JSON: `plugins/Economy/accounts.json`

### 2. 🌐 **Web Dashboard API**
- **Economy API** - просмотр монет игроков
- **Shop API** - управление магазином через браузер
- **AFK API** - настройка AFK системы через браузер

## 📦 Установка

### Шаг 1: Компиляция

```bash
# Windows PowerShell
cd C:\Users\miroslavsky\Downloads\my-polyglot-project\my-polyglot-project
.\gradlew build
```

### Шаг 2: Копирование плагинов

Скопируйте JAR файлы в папку `plugins/` вашего сервера:

```
plugins/
├── economy-0.1.0.jar
├── combat-1.0.0.jar
├── shop-1.0.0.jar
├── afk-1.0.0.jar
└── webx-dashboard-1.0.0.jar
```

### Шаг 3: Запуск сервера

Запустите ваш Paper/Spigot сервер.

## 🧪 Тестирование

### Вариант 1: Через HTML демо (рекомендуется)

1. Откройте файл **`API_DEMO.html`** в браузере
2. Используйте интерфейс для тестирования всех API

### Вариант 2: Через командную строку (curl/PowerShell)

#### 💰 Economy API

**Получить монеты игрока:**
```powershell
$uuid = "YOUR-PLAYER-UUID"
Invoke-RestMethod -Uri "http://localhost:9092/api/player/$uuid/coins"
```

**Топ 10 игроков:**
```powershell
Invoke-RestMethod -Uri "http://localhost:9092/api/players/top?limit=10"
```

#### 🛒 Shop API

**Получить все товары:**
```powershell
Invoke-RestMethod -Uri "http://localhost:9092/api/shop"
```

**Добавить товар:**
```powershell
$item = @{
    id = "unique-id-123"
    name = "Diamond Sword"
    material = "DIAMOND_SWORD"
    price = 100
    icon = $null
} | ConvertTo-Json

Invoke-RestMethod -Uri "http://localhost:9092/api/shop" -Method POST -Body $item -ContentType "application/json"
```

**Удалить товар:**
```powershell
Invoke-RestMethod -Uri "http://localhost:9092/api/shop/unique-id-123" -Method DELETE
```

#### 💤 AFK API

**Получить настройки:**
```powershell
Invoke-RestMethod -Uri "http://localhost:9092/api/afk"
```

**Обновить настройки:**
```powershell
$config = @{
    timeout = 15
    kickEnabled = $true
    kickTimeout = 30
    prefix = "[AFK] "
    suffix = ""
    broadcastEnabled = $true
    autoResumeEnabled = $true
} | ConvertTo-Json

Invoke-RestMethod -Uri "http://localhost:9092/api/afk" -Method PUT -Body $config -ContentType "application/json"
```

## 📊 API Endpoints

### Economy
- `GET /api/player/{uuid}/coins` - Получить монеты игрока
- `GET /api/players/top?limit=10` - Топ игроков

### Shop
- `GET /api/shop` - Список товаров
- `POST /api/shop` - Добавить товар
- `GET /api/shop/{id}` - Получить товар
- `PUT /api/shop/{id}` - Обновить товар
- `DELETE /api/shop/{id}` - Удалить товар

### AFK
- `GET /api/afk` - Получить настройки
- `PUT /api/afk` - Обновить настройки
- `GET /api/afk/players` - Список AFK игроков

## 🎮 Проверка в игре

### 1. Тест начисления монет

1. Зайдите на сервер
2. Убейте любого монстра (зомби, скелет, и т.д.)
3. Вы должны увидеть: `§6+§e1.0 §6coin`
4. Проверьте баланс: `/balance`

### 2. Тест API

1. Получите свой UUID: `/uuid` (если есть такая команда) или из логов
2. Откройте `API_DEMO.html`
3. Вставьте UUID в поле "UUID игрока"
4. Нажмите "🔍 Поиск"
5. Вы должны увидеть свой баланс

### 3. Тест Shop через Dashboard

1. Откройте `API_DEMO.html`
2. Перейдите на вкладку "🛒 Shop"
3. Нажмите "🔄 Обновить" - увидите товары по умолчанию
4. Добавьте новый товар
5. Проверьте файл: `plugins/Shop/shop.json`

### 4. Тест AFK через Dashboard

1. Откройте `API_DEMO.html`
2. Перейдите на вкладку "💤 AFK"
3. Нажмите "🔄 Загрузить текущие настройки"
4. Измените "Таймаут AFK" на 5 минут
5. Нажмите "💾 Сохранить настройки"
6. Проверьте файл: `plugins/AFK/afk.json`

## 📁 Файлы конфигурации

### Economy - accounts.json
```json
{
  "accounts": [
    {
      "uuid": "player-uuid",
      "balance": 1523.5,
      "bankBalance": 500.0
    }
  ]
}
```
Путь: `plugins/Economy/accounts.json`

### Shop - shop.json
```json
[
  {
    "id": "1",
    "name": "Diamond Sword",
    "material": "DIAMOND_SWORD",
    "price": 100.0,
    "icon": null
  }
]
```
Путь: `plugins/Shop/shop.json`

### AFK - afk.json
```json
{
  "timeout": 10,
  "kickEnabled": false,
  "kickTimeout": 30,
  "prefix": "§7[AFK] ",
  "suffix": "",
  "broadcastEnabled": true,
  "autoResumeEnabled": true
}
```
Путь: `plugins/AFK/afk.json`

## 🔧 Troubleshooting

### Ошибка: "Economy plugin not loaded"
- Убедитесь, что плагин Economy загружен первым
- Проверьте логи сервера на ошибки при загрузке Economy

### Ошибка: "Connection refused"
- Проверьте, что сервер запущен
- Проверьте порт 9092 (по умолчанию для WebX Dashboard)
- Откройте порт в firewall

### Shop.json не сохраняется
- Проверьте права доступа к папке `plugins/Shop/`
- Убедитесь, что путь существует

### API возвращает 404
- Проверьте логи WebX Dashboard на наличие ошибок регистрации endpoints
- Убедитесь, что RouterProvider правильно инициализирован

## 📚 Дополнительная документация

- **Полное руководство:** [INTEGRATION_GUIDE.md](INTEGRATION_GUIDE.md)
- **Список плагинов:** [PLUGINS_REGISTRY.md](PLUGINS_REGISTRY.md)
- **Сборка проекта:** [BUILD_ALL.md](BUILD_ALL.md)

## 🎯 Следующие шаги

1. ✅ Протестируйте начисление монет
2. ✅ Протестируйте API через HTML демо
3. ✅ Измените настройки Shop и AFK
4. 📝 Создайте свои эндпоинты по аналогии
5. 🚀 Разверните на production сервере

## 💡 Советы

- Используйте `API_DEMO.html` для быстрого тестирования
- Проверяйте логи сервера при возникновении ошибок
- JSON файлы автоматически создаются при первом запуске
- Для production добавьте аутентификацию к API

---

**Вопросы?** Проверьте [INTEGRATION_GUIDE.md](INTEGRATION_GUIDE.md) для подробной документации.
