# Быстрое решение проблем

## ❌ Ошибка 404 для CSS/JS файлов

```
Failed to load resource: 404
e6ff975709dfa1a4.css
```

**Причина**: Кэш браузера со ссылками на старые файлы  
**Решение**: Ctrl+Shift+R (Hard Reload) в браузере

---

## 🎨 Страница без стилей (только текст)

Страница загружается, но нет цветов и оформления.

**Причина**: Старый JAR на сервере без CSS файлов  
**Решение**:
```bash
# 1. Пересоберите с очисткой
./gradlew clean build

# 2. Проверьте что CSS в JAR
jar -tf webx-dashboard/build/libs/webx-dashboard-1.0.0.jar | grep "\.css$"

# Должны быть файлы:
# web/_next/static/chunks/9146782b78a7b589.css
# web/_next/static/chunks/9f82a1c07477eb5a.css

# 3. Замените JAR на сервере
scp webx-dashboard/build/libs/webx-dashboard-1.0.0.jar user@server:/path/to/plugins/

# 4. ОБЯЗАТЕЛЬНО остановите и запустите сервер заново
```

---

## ⚡ Чек-лист перед деплоем

- [ ] `./gradlew clean build` (с `clean`!)
- [ ] Проверить что JAR содержит CSS: `jar -tf ... | grep css`
- [ ] Остановить сервер перед заменой JAR
- [ ] Заменить JAR файл
- [ ] Запустить сервер
- [ ] Открыть в браузере с Ctrl+Shift+R

---

## 📋 Проверка в логах сервера

После запуска плагина ищите:
```
[WebxDashboard] ✅ CSS files found in classpath
[WebxDashboard] ✅ [HTTP] GET /_next/static/chunks/xxx.css - 200
```

Если видите `❌ 404` - значит:
- Забыли `clean` при сборке
- Не перезапустили сервер после замены JAR
- JAR файл не был заменён

---

## 🔧 Полная переустановка

Если ничего не помогает:
```bash
# 1. Локально
./gradlew clean build

# 2. На сервере
rm /path/to/plugins/webx-dashboard-*.jar
# Перезапустите сервер
# Скопируйте новый JAR
cp webx-dashboard-1.0.0.jar /path/to/plugins/
# Запустите сервер

# 3. В браузере
# Откройте DevTools (F12)
# Application → Storage → Clear site data
# Перезагрузите страницу
```

---

Подробнее: [DEPLOY.md](DEPLOY.md)
