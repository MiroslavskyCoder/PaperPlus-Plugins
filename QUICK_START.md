# 🚀 QUICK START GUIDE - Быстрый старт

**Для разработчиков, которые хотят начать работу с проектом за 5 минут**

---

## 📥 1. Что нужно для запуска

```
✅ Java 25+ (или 17+)
✅ Gradle 9.2.1+ (входит в проект)
✅ Git (опционально)
```

Проверить Java:
```bash
java -version
```

---

## ⚡ 2. Собрать все плагины (одна команда)

```bash
cd my-polyglot-project
gradle buildAllPlugins
```

**Результат:** 13 JAR файлов в папке `out/plugins/`

**Время:** ~26 секунд

---

## 🎯 3. Собрать конкретный плагин

```bash
# Новые плагины (которые только что добавили)
gradle :warps:build
gradle :economy:build
gradle :shop:build
gradle :clans:build
gradle :quests:build

# Старые плагины
gradle :home-tp:build
gradle :back-tp:build
gradle :abomination:build
```

---

## 📂 4. Копирование на сервер

### Windows:
```bash
copy out\plugins\*.jar "C:\Game Servers\Paper\plugins\"
```

### Linux/Mac:
```bash
cp out/plugins/*.jar /opt/gameservers/paper/plugins/
```

---

## 🎮 5. Запустить сервер

```bash
java -Xmx4G -jar paper-1.20.4.jar nogui
```

**Требования:**
- Paper Server 1.20.4 (или выше)
- Минимум 4GB RAM

---

## ⚙️ 6. Конфигурация плагинов

После первого запуска сервера в папке `plugins/` создадутся конфиги:

```
plugins/
├── Warps/config.yml
├── Economy/config.yml
├── Shop/config.yml
├── Clans/config.yml
├── Quests/config.yml
├── HomeTP/config.yml
├── BackTP/config.yml
├── DeathMark/config.yml
└── ... (другие)
```

Отредактируйте нужные конфиги и перезапустите сервер.

---

## 📚 7. Основные команды плагинов

### Warps
```
/warp [name]           - телепортироваться
/setwarp <name>        - создать точку
/delwarp <name>        - удалить точку
/warps                 - список
```

### Economy
```
/balance [player]      - баланс
/pay <player> <amount> - отправить деньги
/baltop                - таблица лидеров
/bank <deposit|withdraw> <amount> - банк
/eco <give|take|set|reset> <player> <amount> - админ
```

### Shop
```
/shop [name]           - открыть магазин
/adminshop create <name>  - создать магазин
/adminshop delete <name>  - удалить магазин
```

### Clans
```
/clan create <name>    - создать клан
/clan invite <player>  - пригласить
/clan kick <player>    - выгнать
/clan rank <player> <rank> - изменить ранг
/clan info             - информация
```

### Quests
```
/quests list           - список квестов
/quests accept <quest> - принять квест
/quests info <quest>   - информация
/quests abandon <quest> - отказаться
```

---

## 🔧 8. Команды Gradle

```bash
# Собрать все
gradle buildAllPlugins

# Очистить сборку
gradle clean

# Собрать и очистить
gradle clean buildAllPlugins

# Проверить ошибки
gradle check

# Список проектов
gradle listProjects

# Помощь
gradle help
```

---

## 📋 9. Структура папок

```
my-polyglot-project/
├── packages/
│   ├── warps/              ← новый (18 файлов)
│   ├── economy/            ← новый (17 файлов)
│   ├── shop/               ← новый (11 файлов)
│   ├── clans/              ← новый (15 файлов)
│   ├── quests/             ← новый (15 файлов)
│   ├── home-tp/
│   ├── back-tp/
│   ├── abomination/
│   ├── death-mark/
│   ├── regionigroks-map/
│   ├── pvp-base/
│   ├── show-health/
│   ├── webx-dashboard/
│   └── webx-dashboard-panel/
│
├── out/
│   └── plugins/            ← сюда копируются JAR файлы
│
├── build.gradle.kts        ← основная сборка
├── settings.gradle.kts     ← регистрация проектов
├── README.md               ← подробная информация
├── PLUGINS_REGISTRY.md     ← реестр всех плагинов
├── IMPLEMENTATION_COMPLETE.md ← статистика
└── PROJECT_COMPLETION.md   ← финальный отчет
```

---

## ❓ 10. Часто задаваемые вопросы

### Q: Как добавить новый плагин?
```bash
# 1. Создать папку
mkdir packages/new-plugin

# 2. Добавить структуру (см. другой плагин)

# 3. В settings.gradle.kts добавить:
include ':new-plugin'
project(':new-plugin').projectDir = file('packages/new-plugin')

# 4. В build.gradle.kts добавить в buildAllPlugins:
dependsOn(':new-plugin:build')

# 5. Собрать
gradle buildAllPlugins
```

### Q: Как изменить конфиг плагина?
```
1. Отредактировать plugins/[PlaginName]/config.yml
2. Перезагрузить плагин или сервер
3. Команда в чате: /reload (если поддерживается)
```

### Q: Как отключить плагин?
```
1. Удалить JAR из папки plugins/
2. Перезагрузить сервер
```

### Q: Как обновить плагин?
```bash
# Пересобрать
gradle :plugin-name:build

# Скопировать новый JAR
copy packages\plugin-name\build\libs\*.jar out\plugins\

# Перезагрузить сервер
```

### Q: Где искать ошибки?
```
logs/latest.log - основной лог сервера
```

---

## 🆘 11. Если что-то не работает

### Ошибка: "gradle command not found"
```bash
# Используйте полный путь или ./gradlew
./gradlew buildAllPlugins
```

### Ошибка: Java not found
```bash
# Установить Java 17+ с java.com
java -version
```

### Сборка не завершается
```bash
# Использовать без daemon
gradle buildAllPlugins --no-daemon
```

### Плагин не загружается
```
1. Проверить logs/latest.log на ошибки
2. Убедиться, что Paper версия 1.20.4+
3. Проверить права доступа на папку plugins/
```

---

## 📖 12. Полная документация

| Файл | Что внутри |
|------|-----------|
| [README.md](README.md) | Полное описание всех плагинов |
| [PLUGINS_REGISTRY.md](PLUGINS_REGISTRY.md) | Реестр и архитектура |
| [IMPLEMENTATION_COMPLETE.md](IMPLEMENTATION_COMPLETE.md) | Статистика проекта |
| [PROJECT_COMPLETION.md](PROJECT_COMPLETION.md) | Финальный отчет |
| [packages/*/README.md](packages) | Для каждого плагина |

---

## 🎯 13. Типичный workflow

```bash
# 1. Клонирование проекта
git clone <repo>
cd my-polyglot-project

# 2. Сборка всех плагинов
gradle buildAllPlugins

# 3. Копирование на сервер
copy out\plugins\*.jar "D:\Servers\Paper\plugins\"

# 4. Запуск сервера
cd "D:\Servers\Paper"
java -Xmx4G -jar paper-1.20.4.jar nogui

# 5. Первая настройка (в чате команды)
/eco give Player1 1000
/setwarp spawn
/adminshop create main

# 6. Готово! 🎉
```

---

## 🚀 14. Performance Tips

```yaml
# В config.yml каждого плагина:

# Включить асинхронные операции
async-operations: true

# Оптимизировать YAML кеширование
cache-enabled: true

# Уменьшить frequency обновлений
update-interval: 300 # seconds

# Использовать MySQL для больших серверов
database:
  type: mysql
  host: localhost
  port: 3306
  username: root
  password: password
  database: minecraft
```

---

## 📱 15. Контакты и ссылки

**Документация:** [README.md](README.md)  
**Плагины:** [out/plugins/](out/plugins/)  
**Исходный код:** [packages/](packages/)

---

**Готово к использованию! 🎮**

Все 13 плагинов (5 новых + 8 старых) скомпилированы и готовы к развертыванию.

Для вопросов смотрите полную документацию в README.md
